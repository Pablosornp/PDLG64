var string texto;
 function pideTexto ()
{
	print ( 'Introduce un texto' );
	prompt (texto);
}
function imprime (string msg, int p, int p1)
{
	print (msg);
}
 pideTexto();
var string textoAux;
textoAux = texto;
imprime (textoAux);
 //Prueba lexico DRACO (NO MODIFICAR)
